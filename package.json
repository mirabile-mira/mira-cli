{
  "name": "mirabile-monorepo",
  "private": true,
  "scripts": {
    "client": "cd client && npm start",
    "server": "cd server && nodemon index.js",
    "dev": "concurrently \"npm run server\" \"npm run client\""
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "dependencies": {
    "mongodb": "^7.1.1"
  }
}
